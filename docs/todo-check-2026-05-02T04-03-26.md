# ToDoList 自動巡檢報告

**巡檢時間**：2026-05-02 04:03 (Asia/Taipei)
**執行者**：TiTi Commander 排程任務
**巡檢方式**：自動讀取 `docs/ToDoList.md`，逐項比對原始碼現況

---

## 巡檢摘要

| 分類 | 未解決數量 | 變化 |
|------|-----------|------|
| 🔴 Feature 缺失 | 5 | 無新增、無解決 |
| 🟠 實作缺失 | 8 | 無新增、無解決 |
| 🟡 空態補強 | 6 | 無新增、無解決 |
| **合計** | **19** | **與上次巡檢一致** |

---

## 🔴 Feature 缺失逐項驗證（5 項）

### 1. `/dashboard` — StudyBuddyBanner（ULTRA 共讀橫幅）無 Feature 覆蓋
- **狀態**：❌ 未解決
- **驗證**：`grep -r StudyBuddy *.feature` → 0 結果。CLAUDE.md 中列出 StudyBuddyBanner 為通用元件，但無任何 Feature Scenario。
- **建議優先級**：P2（UI 尚未渲染，註解 "requires backend /community/online-count API"）

### 2. `/exam/workspace` — Feature 05 AI 打氣語句 UI 未實作（兩條重複記錄）
- **狀態**：❌ 未解決
- **驗證**：`grep -ri '打氣\|encouragement\|cheer\|motivat' frontend/app/exam/workspace/` → 0 結果。頁面無任何打氣語句相關 UI 元素。
- **建議優先級**：P2（Feature 05 描述功能，但實作缺失）

### 3. `/knowledge/mindmap` — 節點點擊互動行為無 Feature Scenario
- **狀態**：❌ 未解決
- **驗證**：`handleNodeClick` 僅執行 `setSelectedNodeId(nodeId)`，無詳情面板或視覺反饋。Feature 文件無覆蓋此互動路徑。
- **建議優先級**：P1（用戶互動核心路徑）

### 4. `/exam/results` — Feature 06 placeholder 規格與已實作狀態不符
- **狀態**：❌ 未解決
- **驗證**：Feature 06 仍描述「LinkedIn 分享 / 下載成績卡片為 placeholder 即將推出」，但程式碼已用 html2canvas 實作下載功能。規格需更新。
- **建議優先級**：P1（規格與實作不同步，回歸測試可能誤判）

### 5. `/account` — 通知偏好 localStorage vs Feature 22 API 操作（規格不同步）
- **狀態**：❌ 未解決
- **驗證**：`frontend/app/account/page.tsx` L95-109 確認使用 `localStorage.getItem/setItem('certimate_notif_*')`，無任何 API 呼叫。Feature 22 規格描述為 API 操作。
- **建議優先級**：P1（規格與實作不同步）

---

## 🟠 實作缺失逐項驗證（8 項）

### 1. `/super-admin/anomaly` — Feature 16「批次修復」缺 UI
- **狀態**：❌ 未解決
- **驗證**：`grep -ri '批次修復\|batch.*fix' frontend/app/super-admin/anomaly/` → 0 結果

### 2. `/knowledge` — 「+ 新增資源」導向 `/dashboard` 而非開啟 modal
- **狀態**：❌ 未解決
- **驗證**：按鈕仍導向 `/dashboard`，Feature 03 無 Scenario 覆蓋此路徑

### 3. `/exam/workspace` — Feature 20 信心度校準 UI（😰😐😎）缺失
- **狀態**：❌ 未解決
- **驗證**：`grep -r '😰\|😐\|😎\|confidence.*icon' frontend/app/exam/` → 0 結果

### 4. `/practice` — Feature 20 信心度校準 UI（😰😐😎）缺失
- **狀態**：❌ 未解決
- **驗證**：practice 頁面有 `信心度 {confidence}%` 文字顯示（L611），但無三圖示 UI。Feature 20 規格要求圖示呈現。

### 5. `/exam/setup` — Bloom 配方面板由 `isAdmin` 守衛而非 `isUltra`
- **狀態**：❌ 未解決
- **驗證**：`page.tsx` L64 解構 `isAdmin`，L699 `{isAdmin && (` 守衛進階面板。ULTRA 付費用戶無法存取。

### 6. `/exam/setup` — SSE 推送未實作，使用假動畫
- **狀態**：❌ 未解決
- **驗證**：`grep -ri 'SSE\|EventSource' frontend/app/exam/setup/` → 0 結果。仍使用 ExamLoadingOverlay 模擬。

### 7. `/account` — 通知偏好需改 API 儲存（與 🔴 #5 同源）
- **狀態**：❌ 未解決
- **驗證**：同上，localStorage 實作確認

### 8. `/exam/workspace` — Feature 05 AI 打氣語句 UI 未實作（與 🔴 #2 同源）
- **狀態**：❌ 未解決

---

## 🟡 空態補強逐項驗證（6 項）

### 1. `/practice` — 未查詢 resource_parse_jobs
- **狀態**：❌ 未解決
- **驗證**：`grep -r 'parse.*job\|failure_reason' frontend/app/practice/` → 0 結果

### 2. `/review` — 未查詢 job 表區分「全答對」vs「job 失敗」
- **狀態**：❌ 未解決
- **驗證**：`grep -r 'job\|failure_reason' frontend/app/review/` → 0 結果

### 3. `/schedule` — 未查 schedule job 表
- **狀態**：❌ 未解決
- **驗證**：`grep -r 'job\|failure_reason' frontend/app/schedule/` → 0 結果

### 4. `/account/weekly-reports` — 未查週報 job 失敗記錄
- **狀態**：❌ 未解決
- **驗證**：`grep -r 'job\|failure_reason' frontend/app/account/weekly-reports/` → 0 結果

### 5. `/knowledge/mindmap` — 空態未查 resource_parse_jobs
- **狀態**：❌ 未解決
- **驗證**：`mindMapNodes.length === 0` 時僅顯示「尚無知識圖譜節點」靜態文字（L153-157），無 job 表查詢。`grep -r 'parse.*job\|failure_reason' frontend/app/knowledge/mindmap/` → 0 結果。

### 6. `/knowledge` — documents 與 nodes 皆空時未查 resource_parse_jobs
- **狀態**：⚠️ 部分解決
- **驗證**：`/knowledge/page.tsx` 已有 `parseJobFailures` state (L72) 及 FAILED 文件的 failure_reason 查詢（L243-251）。但 **documents 與 nodes 皆空且無 FAILED 狀態文件時**，仍顯示靜態提示，不會主動查 job 表。edge case 未完全覆蓋。

---

## 其他待部署項目

- [ ] GCP Billing Export — Cloud Run 環境變數 + Service Account key 掛載（首見：2026-04-17）

---

## CEO 建議

**本次巡檢結論**：19 項未解決問題與上次巡檢一致，無新增也無自動解決。

**優先處理建議（按影響度排序）**：

1. **P0（規格與實作不同步 — 影響回歸測試）**：
   - `/exam/results` Feature 06 placeholder 規格需更新
   - `/account` 通知偏好 localStorage vs API 規格統一

2. **P1（用戶互動核心路徑）**：
   - `/knowledge/mindmap` 節點點擊互動需設計
   - `/exam/setup` Bloom 配方 `isAdmin` → `isUltra` 守衛修正
   - 信心度校準 UI（😰😐😎）於 workspace + practice 頁面

3. **P2（功能補全）**：
   - 空態 Layer 3 補強（6 項）
   - StudyBuddyBanner Feature Scenario
   - SSE 推送替換假動畫
   - AI 打氣語句 UI

---

*下次巡檢預定：2026-05-03 04:00 (Asia/Taipei)*
