# === CertiMate Backend Environment Variables ===
# Copy this file to .env and fill in your values.
# .env is gitignored — keys stay on your machine only.

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/certimate-api_dev

# JWT
JWT_SECRET_KEY=your-secret-key-change-in-production

# CORS — comma-separated allowed origins (empty = allow all)
# ALLOWED_ORIGINS=https://your-app.web.app,https://your-domain.com

# Production mode
# DEBUG=false

# --- AI / LLM API Keys ---
ANTHROPIC_API_KEY=sk-ant-xxx
OPENAI_API_KEY=sk-xxx
GEMINI_API_KEY=AIza-xxx
VOYAGE_API_KEY=pa-xxx

# --- AI Model Defaults (can be overridden by ai_model_routings table) ---
# CLAUDE_MODEL=claude-sonnet-4-20250514
# OPENAI_MODEL=gpt-4o
# GEMINI_MODEL=gemini-2.0-flash

# --- RAG Parameters ---
# CHUNK_SIZE_TOKENS=512
# CHUNK_OVERLAP_TOKENS=64
# RETRIEVAL_TOP_K=10

# 本地開發：設為 claude-code 走 Claude Code CLI subprocess（無 API 費用）
# 雲端部署：不設此 env，走 ANTHROPIC_API_KEY
LLM_LOCAL_BACKEND=
# CLAUDE_CLI_PATH=claude  # CLI 路徑（預設 'claude'，自動從 PATH 找）
